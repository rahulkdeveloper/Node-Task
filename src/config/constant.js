const Roles = {
    user: "user",
    admin: "admin",
    superadmin: "superadmin"
}

const AddedBookStatus = {
    pending: "pending",
    approved: "approved",
    rejected: "rejected"
}

module.exports = { Roles, AddedBookStatus }